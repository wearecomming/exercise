调用change（pth1,pth2）
pth1为提供的数据路径，文本为txt
pth2为输出的推荐列表路径，输出为txt

输入数据如下，是一个二维表
3,5,6,0,0
4,0,6,9,7
9,5,0,3,9

其中用户是纵坐标，热点是横坐标，每个数据的计算为：用户浏览该热点次数+用户是否收藏(是为1，否为0)*5+用户评论数*0.2+用户是否为作者（是为1，否为0）*10


输出数据如下：
[3, 4]
[1]
[2]

每一行代表每一个用户的推荐列表，其中的数据为热点编号（输入数据中的编号），越往前的推荐优先级越高